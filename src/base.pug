//- 
    Базовый файл
    Содержит в себе глобальные переменные,
    а также шаблон страницы HTML

//- Блок переменных для головы страници
-
    const siteTitle = "Моя профессия — Мой выбор!";
    const siteLang = "ru";
    const siteCharset = "UTF-8";


//- Стандартный шаблон страниц
doctype html
html(lang=siteLang)
    head
        meta(charset=siteCharset)
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{siteTitle}
        
        //- СТИЛИ
        link(rel="stylesheet", href="css/style.css")
    body
        #particles-js
        #wrap
            block content
        script(src="js/vendor/particles.min.js" defer)
        script(src="js/particles.js" defer)